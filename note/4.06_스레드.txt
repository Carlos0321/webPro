target이 있는 형태의 스레드]
1.메인은 스레드를 따로 만들지 않아도, 메인을 실행시키면 스레드가 자동생성  
2.스레드라는 객체가 필요 (두가지 일을 동시에 시키려면 각각 스레드 객체필요) 
스레드 객체를 만들기 위해선, 어떤 일을 할 객체인지 “정의”를 내려야함 
2-1) runnalbe을 이용해서 작업을 제일 먼저 설정 해야함(안녕하세요를 정의하는 객체 필요, 반갑습니까 정의하는 객체 필요 마지막에 객체 합쳐서 싹 실행)
 정의 1 : 스레드의 매개변수 안에 Runnable 들어감 (Runnable은 인터페이스 형이라, 객체를 생성못하기 때문에, runnalbe을 임플리 받은 클래스를 만드는 방법 있음 . .. 
   2-2) Runnalbe 에서 임플리 받은 클래스 각각 했으면 , 이 두개를 동시에 실행하는 애(메인함수) 만들기
   2-3) main 에 각각의 클래스의 객체 생성 
   2-4) 동시에 실행하기 위에선 밑에 Thread 객체 각각 더 생성  //고 슛 까지 해줘야 동시 실행 
   2-5) 스레드 변수.start(); 가 고 슛 하는 거임. 각각 객체 변수를 스타트 해주자 
   2-6) 스레드가 총 3개 (내가 만든애 2개, 메인스레드 하나) 
   2-7) 메인 스레드가 존재하는지 확인하기 위해선, for문으로 뿌려주자 .. Thread.currentThread가 메인 스레드 이고, getName을 통해 이름을 뽑아오기… 하지만 메인 스레드에는 아직 초를 설정하지 않아기 때문에 동시 실행 되기 위해선 얘도 Thread.sleep 해주자 
   2-8) 순서는 내가 정하는게 아님.. jvm 이 알아서 스케쥴링해서 짜주는거기 때문에 할때마다 바껴서 나옴 
   2-9) 다 출력된 애는 실행/실행가능 상태에서 완료로 빠짐

synchronized - 먼저 수행되는 스레드의 모든 작업이 끝날 때까지 다른 스레드는 기다리도록 하는 예약
객체에 선언되어 있는 인스턴스 변수를 스레드에서 공유하게되면, 인스턴스 값에 영향을 미치게 되므로 
하나의 자원을 여러 테스크가 사용할때, 한 시점에서 하나의 테스크만 사용할 수 있도록 하기위해 동기화 하는것
