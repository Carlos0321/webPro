브라우저 객체 모델은 웹 브라우저와 관련된 객체의 집합을 의미한다.
이미 많은 객체들이 만들어짐 = 객체 모델 

­window 객체는 브라우저 기반 자바 스크립트의 최상위 객체. 지금까지 함수처럼 사용한
 alert(), prompt()함수 모두 window객체의 메서드이다. Var 키워드로 선언한 일반 변수도 모두 window 객체의 속성이 된다.

// 객체 속성 중 on으로 시작하는 속성을 이벤트 속성이라 하고 함수를 할당해야 함. 
//원래는 onload에 null이 있으나 아래와 같은 로직을 추가하면 해당 이벤트가 발생할 때 해당 함수가 실행
